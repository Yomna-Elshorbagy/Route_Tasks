import express from 'express';

const userRouter = express.Router();

userRouter.get('/', )
userRouter.get('/', )
userRouter.post('/', )
userRouter.put('/', )
userRouter.delete('/', )

export default userRouter;
